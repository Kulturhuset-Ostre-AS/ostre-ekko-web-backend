{{ performance.date|date('D d.F')|replace('. ', ' ') }}<br>
&ensp;{{ performance.time|date("H:i") }}

{# 
{{ artist.date|date('D d.F')|replace('. ', ' ') }}&ensp;<br>
{{ artist.time|date("H:i") }}
 #}